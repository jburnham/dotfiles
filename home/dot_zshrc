# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# If ZSH is not defined, use the current script's directory.
[[ -n "$ZSH" ]] || export ZSH="${${(%):-%x}:a:h}"

source ~/.config/zsh/plugins/zshrc.d/zshrcd.zsh
